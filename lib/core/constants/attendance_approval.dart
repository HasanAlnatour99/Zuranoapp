/// Attendance approval workflow on `salons/{salonId}/attendance/{id}`.
abstract final class AttendanceApprovalStatuses {
  static const pending = 'pending';
  static const approved = 'approved';
  static const rejected = 'rejected';
}
