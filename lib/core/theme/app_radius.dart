class AppRadius {
  const AppRadius._();

  static const double small = 8;
  static const double medium = 12;
  static const double large = 16;

  /// Hero cards, profile shells, and dense marketing strips.
  static const double xlarge = 20;

  /// Bento / dashboard highlight tiles (product spec: 24 logical px).
  static const double bento = 24;

  /// Large profile / barber cards (outer shell).
  static const double profileCard = 28;

  /// Inner photo on profile cards (slightly tighter than [profileCard]).
  static const double profileCardImage = 22;
}
