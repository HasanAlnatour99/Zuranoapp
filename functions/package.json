{
  "name": "barber-shop-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc --watch",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:emulators": "cd .. && firebase emulators:exec --only firestore,functions,auth --project demo-test \"npm --prefix functions test\""
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.0",
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.3.0",
    "luxon": "^3.7.2"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "@types/luxon": "^3.7.1",
    "@types/node": "^20.19.0",
    "firebase": "^12.12.0",
    "firebase-functions-test": "^3.4.1",
    "typescript": "^5.7.3",
    "vitest": "^4.1.4"
  }
}
